{
  "name": "jira-service-management-queue-page-ui-kit",
  "version": "1.0.15",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "./node_modules/.bin/eslint src/**/* || npm run --silent hook-errors",
    "hook-errors": "echo '\\x1b[31mThe build failed because a Forge UI hook is being used incorrectly. Forge UI hooks follow the same rules as React Hooks but have their own API definitions. See the Forge documentation for details on how to use Forge UI hooks.\n' && exit 1",
    "forge:install": "forge install -p Jira -s nuvs.atlassian.net",
    "deploytunnel:win": "cd static/queue-page && rmdir /s build && npm run build && cd ../../ && forge deploy && forge tunnel"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "eslint-plugin-react-hooks": "^4.6.0"
  },
  "dependencies": {
    "@forge/api": "^2.19.1",
    "@forge/resolver": "^1.5.17",
    "@forge/ui": "1.9.5"
  }
}
